[tools]
go = "1.26.1"
golangci-lint = "2.11.3"

[tasks.install]
description = "Build and install the plugin to ~/.tflint.d/plugins"
run = """
go build
mkdir -p ~/.tflint.d/plugins
mv ./tflint-ruleset-sheldon ~/.tflint.d/plugins/tflint-ruleset-sheldon
"""

[settings]
lockfile = true
