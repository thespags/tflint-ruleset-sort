# Rule `sort_unknown_resource`

Warns when a resource or module is encountered that is not defined in the
plugin configuration. This helps identify resources that may need
key-attributes configured.

## Example

```text
Warning: unknown resource `aws_instance` — consider adding it to the sort plugin configuration (sort_unknown_resource)
```
