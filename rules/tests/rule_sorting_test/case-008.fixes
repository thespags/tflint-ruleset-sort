# Test: Multi-liner key sorting in objects (two multi-line values out of order)
resource "example" "this" {
  tags = {
    "aaa" = jsonencode({
      baz = "qux"
    })

    "zzz" = jsonencode({
      foo = "bar"
    })
  }
}
